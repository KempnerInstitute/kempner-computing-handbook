[project]
name = "kempner-computing-handbook"
version = "0"
requires-python = ">=3.12"
dependencies = [
    "jupyter-book<2",
    "sphinx-sitemap>=2.9.0",
    "sphinxcontrib-mermaid>=2.0.2",
    "sphinxext-opengraph>=0.13.0",
]

[tool.uv]
package = false
